<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PostMapper">   
  
	
   
	<select id="postListByAddr" parameterType="string" resultType="PostDTO">
		select pNum,userid,addr,pCategory,pTitle,pContent,pPrice,pImage,pHit,pDate,TRUNC((SYSDATE-PDATE) * 24 * 60) AS PDATEDIFF
       	from post
       	where addr = #{addr}
       	order by pDate desc 
       	
	</select>
	
	<select id="postListAll" resultType="PostDTO">
       select PNUM
    , USERID
    , ADDR
    , PCATEGORY
    , PTITLE
    , PCONTENT
    , PPRICE
    , PIMAGE
    , PHIT
    , PDATE 
    ,TRUNC((SYSDATE-PDATE) * 24 * 60) AS PDATEDIFF
       from post
       order by pDate desc
        
	</select>	
	
	<select id="getPostByPNum" parameterType="int" resultType="PostDTO">
		select * 
		from post
		WHERE pNum=${pNum}
	</select>
	
	<update id="updatePost" parameterType="PostDTO">
		update post 
		set pTitle=#{pTitle},pContent=#{pContent},pPrice=#{pPrice},pImage=#{pImage}, pDate=SYSDATE
		where pNum=#{pNum}
	</update>
	
	<update id="updatePHit" parameterType="PostDTO">
		update post 
		set pHit=#{pHit}
		where pNum=#{pNum}
	</update>
	
	<delete id="deletePostByPNum" parameterType="int">
		delete 
		from post
		where pNum=#{pNum}
	</delete>
	
	<insert id="newPost" parameterType="PostDTO">
		INSERT INTO post
		VALUES (POST_NUM.NEXTVAL, #{userid}, #{addr}, #{pCategory}, #{pTitle}, #{pContent}, #{pPrice}, #{pImage}
		,#{pHit}, SYSDATE, #{pStatus} )
	</insert>
</mapper>